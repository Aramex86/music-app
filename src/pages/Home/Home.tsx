import { Box } from "components";
import { Colors } from "helpers/enums";

export const Home = () => {
  return (
    <Box
      width="100%"
      backgroundColor={`${Colors.Mirage}`}
      color={`${Colors.White}`}
    >
      HOME
    </Box>
  );
};
